# JWT Secret for token signing (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Email configuration for password reset and notifications
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Base URL for your application
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database configuration
# MongoDB Atlas (recommended for production)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/berrytrip?retryWrites=true&w=majority

# Alternative: PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost:5432/berrytrip

# If neither MONGODB_URI nor DATABASE_URL is set, the app will use in-memory database
